# frontend

Aplicación React + Vite del Modelo Predictivo de Compras.

**Estado:** carpeta recién creada en Fase 1. La migración del código desde la raíz del proyecto se hace en el Paso 2.

Cuando esté lista:

```bash
npm install
npm run dev    # puerto 3000, proxy /api → http://localhost:3001
```
