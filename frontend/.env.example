# Variables de build del frontend.

# URL pública del backend.
# - Local: dejar vacío para que el proxy de Vite (vite.config.ts) redirija /api/* al backend en :3001.
# - Producción: URL absoluta del backend (App Runner) en .env.production.
VITE_API_URL=
